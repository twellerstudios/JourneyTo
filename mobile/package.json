{
  "name": "journeyto-poster-mobile",
  "version": "0.1.0",
  "description": "Journey To Poster — create, edit and publish WordPress posts to letsjourneyto.com from your phone",
  "private": true,
  "scripts": {
    "start": "npx serve www -l 8100",
    "android:init": "npx cap add android && node scripts/patch-android.js",
    "android:sync": "npx cap sync android && node scripts/patch-android.js",
    "android:open": "npx cap open android",
    "android:run": "npx cap run android",
    "android:patch": "node scripts/patch-android.js"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/browser": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/preferences": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "serve": "^14.0.0"
  }
}
